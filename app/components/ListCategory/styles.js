import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 24,
        fontWeight: '600',
    },
    list: {
        paddingTop: 16,
    }
});
export default styles;